export const selectOptions = (state) => state.options;
export const selectWordLength = (state) => state.options.wordLength;
export const selectNumGuesses = (state) => state.options.numGuesses;
export const selectIsHardMode = (state) => state.options.isHardMode;
