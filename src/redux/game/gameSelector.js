export const selectGameState = (state) => state.gameState;
export const selectSolution = (state) => state.gameState.solution;
export const selectRows = (state) => state.gameState.rows;
export const selectKeyColors = (state) => state.gameState.keyColors;
export const selectActiveRow = (state) => state.gameState.activeRow;
export const selectTimer = (state) => state.gameState.timer;
export const selectChallengeID = (state) => state.gameState.challengeID;
